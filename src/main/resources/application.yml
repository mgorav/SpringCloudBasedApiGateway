logging:
  level:
    org.springframework.cloud.gateway: TRACE
server:
  port: 9090


hystrix:
  command:
    HystrixSlowCommand:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000